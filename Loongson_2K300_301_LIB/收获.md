1. 由printf的数据知道编码器线接错了
2. 速度环（编码器） - 角速度环（IMU） - 位置环（摄像头）
3. Kp -- 速度给零，扰动车体看现象
4. Kp > Ki
5. 可以用vofa（速度环看波形）
6. motor.cpp 太乱（变量重复赋值，魔数很多等），代码耦合度太低了。
   - 建议：
     1. var.h , vofa.h/cpp
     2. PIDStruct -> OOP，是对应环的对象啊
     3. 

<!-- #include "lq_timer.hpp"

// 1. 定义回调函数（无参无返回值）
void my_callback()
{
    printf("定时器触发！\n");
}

// 2. 创建定时器对象
lq_timer timer;

// 3. 启动定时器（二选一）
timer.set_seconds_s(1, my_callback);      // 每 1 秒触发一次
// timer.set_seconds_ms(500, my_callback); // 每 500 毫秒触发一次

// 4. 停止定时器
timer.stop(); -->

##下面的你可以不用看

#拉取官方推送
git pull official master 

#to newlib
git checkout newlib //切换分支
git add .  (or ./../git_add.sh)
git commit -m "description"
git push -u ours newlib


#杂谈
git rm -r --cached .vscode //停止git跟踪.vscode  这与git add -r .vscode形成互逆过程
git log --oneline 看提交时间线
还有git回退到某个commit和撤销commit